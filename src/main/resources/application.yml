
spring:
  application:
    name: lab-1-boot
  jpa:
    hibernate:
      ddl-auto: update
  mvc:
    contentnegotiation:
      favor-parameter: true

management:
  endpoints:
    web:
      exposure:
        include: info,health,beans
